'use strict';

var firstAndPike = {
  min: 23,
  max: 65,
  checkAverage: function() {
    return (this.min + this.max) / 2;
  }
};

console.log(firstAndPike.checkAverage);

var seatacAirport = {
  min: 23,
  max: 65,
  checkAverage: function() {
    return (this.min + this.max) / 2;
  }
};

console.log(seatacAirport.checkAverage);

var seattleCenter = {
  min: 23,
  max: 65,
  checkAverage: function() {
    return (this.min + this.max) / 2;
  }
};

console.log(seattleCenter.checkAverage);

var capitolHill = {
  min: 23,
  max: 65,
  checkAverage: function() {
    return (this.min + this.max) / 2;
  }
};

console.log(capitolHill.checkAverage);

var alki = {
  min: 23,
  max: 65,
  checkAverage: function() {
    return (this.min + this.max) / 2;
  }
};
  
console.log(alki.checkAverage);